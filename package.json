{
  "name": "copilotkit-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.13",
    "vitest": "1.5.0",
    "wrangler": "^3.65.1"
  },
  "dependencies": {
    "@copilotkit/runtime": "^1.0.9",
    "class-validator": "^0.14.1"
  }
}
